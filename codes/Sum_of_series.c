#include<stdio.h>
int n;
int series(int n)
{
    if(n==0)
    {
        int main();
    }
    else

    return n+series(n-1);
}

int main()
{
    printf("Enter the integer : \n");
    scanf("%d",&n);

    printf("=%d",series(n));

    return 0;

}
